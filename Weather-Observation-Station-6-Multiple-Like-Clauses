/*
When doing multiple like statements, bracket the whole thing and you have to type the column name each time.
*/

SELECT DISTINCT CITY FROM STATION
WHERE (CITY LIKE 'a%'
       OR CITY LIKE 'e%'
       OR CITY LIKE 'i%'
       OR CITY LIKE 'o%'
       OR CITY LIKE 'u%')
